# =============================================================================
# DebrisLink — Production Dependencies
# =============================================================================
# Pinned with floors only; the cloud build will resolve patch versions.
# After your first successful deploy, lock these with `pip freeze` into
# requirements.lock for reproducible builds.
# =============================================================================

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Data validation & settings ---
pydantic>=2.6
pydantic[email]>=2.6
pydantic-settings>=2.2

# --- Database ---
sqlalchemy>=2.0
psycopg2-binary>=2.9          # PostgreSQL driver (precompiled — no system libs needed)

# --- HTTP client (Meta WhatsApp Cloud API) ---
httpx>=0.27

# --- PDF generation ---
reportlab>=4.0
